diff --git a/xiaohongshu/feed_detail.go b/xiaohongshu/feed_detail.go
index 2cfde9f..25d3cb6 100644
--- a/xiaohongshu/feed_detail.go
+++ b/xiaohongshu/feed_detail.go
@@ -1001,5 +1001,5 @@ func (f *FeedDetailAction) extractFeedDetail(page *rod.Page, feedID string) (*Fe
 }
 
 func makeFeedDetailURL(feedID, xsecToken string) string {
-	return fmt.Sprintf("https://www.xiaohongshu.com/explore/%s?xsec_token=%s&xsec_source=pc_feed", feedID, xsecToken)
+	return fmt.Sprintf("%s/explore/%s?xsec_token=%s&xsec_source=pc_feed", siteBaseURL, feedID, xsecToken)
 }
diff --git a/xiaohongshu/login.go b/xiaohongshu/login.go
index 67e002d..f07b655 100644
--- a/xiaohongshu/login.go
+++ b/xiaohongshu/login.go
@@ -13,6 +13,14 @@ type LoginAction struct {
 	page *rod.Page
 }
 
+const siteBaseURL = "https://www.rednote.com"
+
+const loggedInJS = `() => {
+	const u = window.__INITIAL_STATE__ && window.__INITIAL_STATE__.user;
+	const info = u && u.userInfo && u.userInfo.value !== undefined ? u.userInfo.value : (u && u.userInfo);
+	return Boolean(info && !info.guest);
+}`
+
 func NewLogin(page *rod.Page) *LoginAction {
 	return &LoginAction{page: page}
 }
@@ -20,20 +28,15 @@ func NewLogin(page *rod.Page) *LoginAction {
 func (a *LoginAction) CheckLoginStatus(ctx context.Context) (bool, error) {
 	// 加超时保护：只是查登录态的快速检查，不应无限挂（登录扫码的等待在 Login/WaitForLogin 里）
 	pp := a.page.Context(ctx).Timeout(30 * time.Second)
-	pp.MustNavigate("https://www.xiaohongshu.com/explore").MustWaitLoad()
+	pp.MustNavigate(siteBaseURL + "/explore").MustWaitLoad()
 
 	time.Sleep(1 * time.Second)
 
-	exists, _, err := pp.Has(`.main-container .user .link-wrapper .channel`)
+	result, err := pp.Eval(loggedInJS)
 	if err != nil {
 		return false, errors.Wrap(err, "check login status failed")
 	}
-
-	if !exists {
-		return false, errors.Wrap(err, "login status element not found")
-	}
-
-	return true, nil
+	return result.Value.Bool(), nil
 }
 
 // CurrentUser 当前登录用户的基础信息。
@@ -74,11 +77,11 @@ func (a *LoginAction) Login(ctx context.Context) error {
 	pp := a.page.Context(ctx)
 
 	// 导航到小红书首页，这会触发二维码弹窗
-	pp.MustNavigate("https://www.xiaohongshu.com/explore").MustWaitLoad()
+	pp.MustNavigate(siteBaseURL + "/explore").MustWaitLoad()
 
 	time.Sleep(2 * time.Second)
 
-	if exists, _, _ := pp.Has(".main-container .user .link-wrapper .channel"); exists {
+	if result, err := pp.Eval(loggedInJS); err == nil && result.Value.Bool() {
 		return nil
 	}
 
@@ -91,11 +94,11 @@ func (a *LoginAction) FetchQrcodeImage(ctx context.Context) (string, bool, error
 	pp := a.page.Context(ctx)
 
 	// 导航到小红书首页，这会触发二维码弹窗
-	pp.MustNavigate("https://www.xiaohongshu.com/explore").MustWaitLoad()
+	pp.MustNavigate(siteBaseURL + "/explore").MustWaitLoad()
 
 	time.Sleep(2 * time.Second)
 
-	if exists, _, _ := pp.Has(".main-container .user .link-wrapper .channel"); exists {
+	if result, err := pp.Eval(loggedInJS); err == nil && result.Value.Bool() {
 		return "", true, nil
 	}
 
@@ -120,8 +123,8 @@ func (a *LoginAction) WaitForLogin(ctx context.Context) bool {
 		case <-ctx.Done():
 			return false
 		case <-ticker.C:
-			el, err := pp.Element(".main-container .user .link-wrapper .channel")
-			if err == nil && el != nil {
+			result, err := pp.Eval(loggedInJS)
+			if err == nil && result.Value.Bool() {
 				return true
 			}
 		}
diff --git a/xiaohongshu/search.go b/xiaohongshu/search.go
index 7c22952..6a4dbdf 100644
--- a/xiaohongshu/search.go
+++ b/xiaohongshu/search.go
@@ -256,5 +256,5 @@ func makeSearchURL(keyword string) string {
 
 	//https://www.xiaohongshu.com/search_result?keyword=%25E7%258E%258B%25E5%25AD%2590&source=web_search_result_notes
 	//https://www.xiaohongshu.com/search_result?keyword=%25E7%258E%258B%25E5%25AD%2590&source=web_explore_feed
-	return fmt.Sprintf("https://www.xiaohongshu.com/search_result?%s", values.Encode())
+	return fmt.Sprintf("%s/search_result?%s", siteBaseURL, values.Encode())
 }
