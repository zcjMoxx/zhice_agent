The user explicitly selected `/subagent once` for this message. First call `discover_tools` to activate `delegate_tasks`, then call `delegate_tasks` exactly once before giving the final answer. Use `reason="explicit_user_request"`. Choose one child when there is only one useful isolated or specialist task; do not invent multiple dependent tasks. If discovery reports that delegation is unavailable, call the returned `delegate_tasks` capability facade and report its exact cause instead of substituting another Tool. If delegation fails, report that fact instead of silently answering as though delegation occurred. This requirement does not change Tool, Profile, permission, confirmation, Hook, workspace, timeout, or depth limits.
