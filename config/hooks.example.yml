# Copy to ${ZHICE_AGENT_WORKSPACE}/config/hooks.yml to enable local Tool Hooks.
version: 1

hooks: []

# Example:
# hooks:
#   - name: restrict-exec
#     stage: pre_tooluse
#     script: extends/hooks/restrict_exec.py
#     tools: [exec]
#     exempt_roles: [owner]
#     exempt_permissions: [tool.exec.dangerous]
#     timeout_seconds: 2
#     max_output_chars: 16384
