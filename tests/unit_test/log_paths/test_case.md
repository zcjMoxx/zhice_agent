# Workspace 日志路径测试说明

## 测试目标

验证结构化运行日志的唯一权威每日文件名。

## 用例覆盖

- 指定日期生成扁平的 `logs/log-YYYY-MM-DD.jsonl` 路径。
- UTC 时间统一按固定北京时间 `UTC+08:00` 计算展示和每日文件日期，不依赖宿主机或容器 timezone。

## 关键检查点

- 新写入路径扩展名必须是 `.jsonl`。
- 不保留旧路径兼容分支。
- 北京时间跨日边界必须稳定输出 `+08:00`。
